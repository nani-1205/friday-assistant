# .env file

# --- Google AI Studio ---
# Get your key from https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=YOUR_GOOGLE_AI_STUDIO_API_KEY

# --- MongoDB Configuration ---
# Replace with your MongoDB Atlas or local instance details
MONGO_USER=your_mongo_user
MONGO_PASSWORD=your_mongo_password_with_special_chars_like_@ # Ensure this is URL-encoded if needed, but the app handles it now
MONGO_HOST=your_mongo_host_or_ip_or_atlas_cluster_url # e.g., localhost OR cluster0.xxxxx.mongodb.net
MONGO_PORT=27017 # Default port (ignored for mongodb+srv:// URIs)
MONGO_DB_NAME=friday_assistant_db
MONGO_COLLECTION_NAME=interactions
MONGO_AUTH_DB=admin # Often 'admin' or the same as MONGO_DB_NAME for Atlas

# --- WeatherAPI.com Configuration ---
# Get your key from https://www.weatherapi.com
# IMPORTANT: Generate a NEW key - the one shared earlier is compromised.
WEATHER_API_KEY=YOUR_NEW_WEATHERAPI_COM_API_KEY

# --- Flask Configuration ---
# Set FLASK_DEBUG=0 for production deployments
FLASK_DEBUG=1 # 1 for development (enables debug mode), 0 for production